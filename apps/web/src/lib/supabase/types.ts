/**
 * Supabase client typing — generated schema types only.
 * Domain models stay in `@regapro/database` schemas / `@regapro/shared`.
 */
export type {
  Database,
  Json,
  Tables,
  TablesInsert,
  TablesUpdate,
} from "@regapro/database";
