# Generated Supabase types

`database.types.ts` is produced from the linked Supabase project schema.

```bash
npm run db:gen-types
```

These types describe the Postgres / PostgREST shape only. Application domain
models and Zod schemas remain in `../schemas.ts` and `@regapro/shared`.
