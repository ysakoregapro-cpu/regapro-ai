{
  "name": "@regapro/research-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@regapro/database": "*",
    "@regapro/research": "*",
    "@regapro/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
